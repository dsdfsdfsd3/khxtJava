<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lz.mapper.PositionMapper">
    
    <resultMap id="PositionResultMap" type="com.lz.bean.Position">
        <id property="id" column="id"/>
        <result property="postName" column="post_name"/>
        <result property="postCode" column="post_code"/>
        <result property="description" column="description"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <select id="findAll" resultMap="PositionResultMap">
        SELECT * FROM position ORDER BY sort_order ASC, id ASC
    </select>
    
    <select id="findById" resultMap="PositionResultMap">
        SELECT * FROM position WHERE id = #{id}
    </select>
    
    <select id="findByName" resultMap="PositionResultMap">
        SELECT * FROM position WHERE post_name = #{postName}
    </select>
    
    <insert id="insert" parameterType="com.lz.bean.Position" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO position (post_name, post_code, description, sort_order, status)
        VALUES (#{postName}, #{postCode}, #{description}, #{sortOrder}, #{status})
    </insert>
    
    <update id="update" parameterType="com.lz.bean.Position">
        UPDATE position
        <set>
            <if test="postName != null">post_name = #{postName},</if>
            <if test="postCode != null">post_code = #{postCode},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM position WHERE id = #{id}
    </delete>
    
    <select id="countByPositionId" resultType="int">
        SELECT COUNT(*) FROM employee WHERE position_id = #{positionId}
    </select>
    
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lz.mapper.DepartmentMapper">
    
    <resultMap id="DepartmentResultMap" type="com.lz.bean.Department">
        <id property="id" column="id"/>
        <result property="deptName" column="dept_name"/>
        <result property="deptCode" column="dept_code"/>
        <result property="parentId" column="parent_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    
    <select id="findAll" resultMap="DepartmentResultMap">
        SELECT * FROM department ORDER BY sort_order ASC, id ASC
    </select>
    
    <select id="findById" resultMap="DepartmentResultMap">
        SELECT * FROM department WHERE id = #{id}
    </select>
    
    <select id="findByName" resultMap="DepartmentResultMap">
        SELECT * FROM department WHERE dept_name = #{deptName}
    </select>
    
    <insert id="insert" parameterType="com.lz.bean.Department" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO department (dept_name, dept_code, parent_id, sort_order, status)
        VALUES (#{deptName}, #{deptCode}, #{parentId}, #{sortOrder}, #{status})
    </insert>
    
    <update id="update" parameterType="com.lz.bean.Department">
        UPDATE department
        <set>
            <if test="deptName != null">dept_name = #{deptName},</if>
            <if test="deptCode != null">dept_code = #{deptCode},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM department WHERE id = #{id}
    </delete>
    
    <select id="countByParentId" resultType="int">
        SELECT COUNT(*) FROM department WHERE parent_id = #{parentId}
    </select>
    
</mapper>
